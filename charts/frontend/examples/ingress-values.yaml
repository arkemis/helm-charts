# DEPRECATED: ingress-nginx setup, kept as a migration reference. See gateway-values.yaml.

image:
  repository: arkemishub/frontend
  tag: "1.0.0"

ingress:
  enabled: true
  ingressClassName: nginx
  certIssuer: cert-manager-global
  hosts:
    - app.example.com
