kubernetesClusterDomain: cluster.local
namespace: ""
certIssuer: ""
deployment:
  replicas: 1
  image: ""
  imagePullSecret: ""
  restartPolicy: Always
  resources:
    limits:
      memory: 1Gi
    requests:
      memory: 512Mi
service:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
configMap:
  env: {}
ingress:
  hosts: []
