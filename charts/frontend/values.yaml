kubernetesClusterDomain: cluster.local
namespace: ""
certIssuer: ""
deployment:
  replicas: 1
  image: ""
  imagePullSecret: ""
  restartPolicy: Always
  resources:
    requests:
      memory: "1Gi"
      cpu: "1000m"
    limits:
      memory: "2Gi"
      cpu: "1000m"
service:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
configMap:
  env: {}
ingress:
  hosts: []
