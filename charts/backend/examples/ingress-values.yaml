# DEPRECATED: ingress-nginx setup, kept as a migration reference. See gateway-values.yaml.

image:
  repository: arkemishub/backend
  tag: "1.0.0"

ingress:
  enabled: true
  ingressClassName: nginx
  certIssuer: cert-manager-global
  hosts:
    - api.example.com

internalIngress:
  enabled: true
  whitelistSourceRange: "10.224.0.0/14"
